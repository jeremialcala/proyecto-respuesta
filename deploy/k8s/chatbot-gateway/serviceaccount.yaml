apiVersion: v1
kind: ServiceAccount
metadata:
  name: chatbot-gateway
  annotations:
    # IRSA: sqs:Receive/Delete en inbound-text, sqs:SendMessage en outbound-reply y report-received.
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/respuesta-chatbot-gateway
