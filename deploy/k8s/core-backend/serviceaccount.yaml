apiVersion: v1
kind: ServiceAccount
metadata:
  name: core-backend
  annotations:
    # IRSA: sqs:Receive/Delete en report-received; sns:Publish en report-ingested y state-changed.
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/respuesta-core-backend
