apiVersion: v1
kind: ServiceAccount
metadata:
  name: vault-worker
  annotations:
    # IRSA: sqs:Receive/Delete en inbound-media, sns:Publish en media-stored, s3:PutObject en la
    # bóveda/cuarentena y kms:GenerateDataKey en la KEK (least-privilege, ADR-0008).
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/respuesta-vault-worker
