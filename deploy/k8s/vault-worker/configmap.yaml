apiVersion: v1
kind: ConfigMap
metadata:
  name: vault-worker-config
data:
  AWS_REGION: "sa-east-1"
  SQS_INPUT_QUEUE_URL: "https://sqs.sa-east-1.amazonaws.com/ACCOUNT_ID/inbound-media"
  SQS_STORED_QUEUE_URL: "arn:aws:sns:sa-east-1:ACCOUNT_ID:media-stored"
  MEDIA_BUCKET: "respuesta-media-saeast1"
  QUARANTINE_BUCKET: "respuesta-quarantine-saeast1"
  GRAPH_API_BASE: "https://graph.facebook.com/v20.0"
