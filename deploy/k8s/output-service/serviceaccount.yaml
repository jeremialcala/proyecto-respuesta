apiVersion: v1
kind: ServiceAccount
metadata:
  name: output-service
  annotations:
    # IRSA: sqs:Receive/Delete en outbound-reply (least-privilege).
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/respuesta-output-service
