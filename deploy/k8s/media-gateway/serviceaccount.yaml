apiVersion: v1
kind: ServiceAccount
metadata:
  name: media-gateway
  annotations:
    # IRSA: kms:GenerateMac/VerifyMac (HMAC del token), kms:Decrypt + s3:GetObject (servir la foto).
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/respuesta-media-gateway
