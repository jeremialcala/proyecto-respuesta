apiVersion: v1
kind: Service
metadata:
  name: media-gateway-internal
spec:
  selector: {app.kubernetes.io/name: media-gateway-internal}
  # Puerto 8081 = el default que el output-service espera (MEDIA_GATEWAY_URL). Sin Ingress (mesh privado).
  ports: [{port: 8081, targetPort: http, name: http}]
