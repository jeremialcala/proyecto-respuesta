apiVersion: v1
kind: ServiceAccount
metadata:
  name: inference-worker
  annotations:
    # IRSA MÍNIMA (ADR-0019 §4: sin secretos en el nodo). Solo sqs:Receive/Delete sobre
    # face-extract-requested y sns:Publish sobre face-embedded. SIN acceso a Vault/KMS/pgvector/S3.
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/respuesta-inference-worker
