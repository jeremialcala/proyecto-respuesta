apiVersion: v1
kind: ServiceAccount
metadata:
  name: webhook-gateway
  annotations:
    # IRSA: rol con permiso sqs:SendMessage sobre la cola meta-received (least-privilege).
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/respuesta-webhook-gateway
